package com.fges;

public interface Command {
    int execute();

    // Integer = Objet autour de Int donc plusieurs avantages comme le fait qu'il puisse etre null et tout mais + lourd que int. int c un type primitif donc pour choses primitives. *

}
